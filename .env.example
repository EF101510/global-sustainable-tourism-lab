# Anthropic API key — used by the /api/chat proxy
# (both the Vite dev middleware and the Vercel serverless function).
# Copy this file to .env.local and fill in your real key.
# Never commit .env or .env.local — see .gitignore.
ANTHROPIC_API_KEY=
